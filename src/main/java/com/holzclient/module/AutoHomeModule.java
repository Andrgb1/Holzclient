package com.holzclient.module;

import net.minecraft.client.MinecraftClient;
import net.minecraft.util.math.BlockPos;

public class AutoHomeModule extends Module {
    // AutoHome module removed. Previously contained commands that could affect multiplayer servers.
    // The file is intentionally left empty to preserve class references if needed.
}
